# 🌅 PAGE X

Современная и персонализируемая домашняя страница с анимированным фоном и интеллектуальными функциями.

## ✨ Особенности

- 🌌 **Анимированный фон Solar System** - красивый анимированный фон с движущимися планетами
- 🌍 **Умная погода** - автоматическое определение местоположения и отображение текущей погоды
- 🎨 **Автоматические иконки сайтов** - автоматическая загрузка иконок для всех ссылок
- ⚙️ **Система настроек** - полная персонализация интерфейса через удобную панель
- 🔍 **Умный поиск** - поддержка множественных поисковых систем
- 📱 **Адаптивный дизайн** - отлично работает на всех устройствах
- 🎯 **Горячие клавиши** - быстрая навигация с клавиатуры
- 💾 **Сохранение настроек** - автоматическое сохранение в localStorage
- 📊 **Статусная строка** - время, погода, курс криптовалют

## 🚀 Быстрый старт

### Предварительные требования

Убедитесь, что у вас установлен [Node.js](https://nodejs.org/) версии 14.0.0 или выше.

### Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/your-username/PAGE-X.git
   cd PAGE-X
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Запустите сервер:**
   
   **Для разработки (с автоматической перезагрузкой):**
   ```bash
   npm run dev
   ```
   
   **Для продакшена:**
   ```bash
   npm start
   ```

4. **Откройте в браузере:**
   ```
   http://localhost:8000
   ```

### Дополнительные команды

- **Проверка состояния сервера:** `http://localhost:8000/health`
- **Установка зависимостей:** `npm run install-deps`

## ⚙️ Настройки

### Панель настроек

Нажмите кнопку ⚙️ в правом верхнем углу или клавишу `S` для открытия панели настроек.

В текущей версии панель настроек позволяет управлять следующими параметрами:

#### 🌌 Фон (Солнечная система)
- **Включение/отключение** анимированного фона.
- **Скорость анимации** - настройка скорости движения планет (0.5x - 3x).
- **Прозрачность фона** - настройка прозрачности анимированного фона (0-100%).

Дальнейшие настройки (темы, погода, поиск, вкладки) будут добавлены в будущих обновлениях.

### Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `S` | Открыть/закрыть панель настроек |
| `B` | Переключить анимированный фон (Солнечная система) |
| `1-9` | Переключение вкладок |
| `Esc` | Закрыть панели |
| `G`, `D`, `Y`, `H` | Быстрое переключение поисковых систем |

### Настройка погоды

#### Автоматическое определение местоположения
1. **Разрешите доступ** к местоположению в браузере
2. **Дождитесь определения** города
3. **Погода автоматически загрузится**

#### Ручной ввод города
1. **Кликните по виджету погоды** в статусной строке
2. **Введите название города** (например: "Moscow", "London, GB")
3. **Нажмите Enter** для сохранения

#### Используемый API
Проект использует **Open-Meteo API** - полностью бесплатный сервис:
- ✅ **Без регистрации** - не требует API ключ
- ✅ **Без ограничений** - неограниченное количество запросов
- ✅ **Высокая точность** - данные от ведущих метеослужб

Подробная документация: [docs/WEATHER_SETUP.md](docs/WEATHER_SETUP.md)

## 📁 Структура проекта

```
PAGE X/
├── index.html                 # Главная страница
├── userconfig.js             # Конфигурация пользователя
├── src/
│   ├── components/           # Web Components
│   │   ├── solar-system.js   # Анимированный фон
│   │   ├── search-bar.js     # Поисковая панель
│   │   ├── tabs-container.js # Контейнер вкладок
│   │   ├── icon-loader.js    # Загрузчик иконок
│   │   ├── settings-panel.js # Панель настроек
│   │   ├── status-bar.js     # Статусная строка (время, погода, крипто)
│   │   ├── quick-search.js   # Быстрый поиск
│   │   └── todo-panel.js     # Панель задач
│   ├── scripts/
│   │   └── app.js           # Основной скрипт приложения
│   └── styles/
│       ├── main.css         # Основные стили
│       └── gruvbox.css      # Тема Gruvbox
├── docs/                    # Документация
│   └── WEATHER_SETUP.md    # Настройка погоды
└── test-*.html             # Тестовые страницы
```

## 🧪 Тестирование

### Основные тесты
- **Главная страница:** `http://localhost:8000`
- **Тест настроек:** `http://localhost:8000/test-settings.html`
- **Тест иконок:** `http://localhost:8000/test-icons.html`
- **Тест солнечной системы:** `http://localhost:8000/test-solar.html`

### Проверка функций
1. **Настройки** - откройте панель настроек и измените параметры
2. **Поиск** - попробуйте разные поисковые системы
3. **Погода** - кликните по виджету погоды для изменения города
4. **Фон** - переключите анимированный фон клавишей `B`
5. **Вкладки** - переключайтесь между вкладками клавишами `1-9`
6. **Иконки** - проверьте загрузку иконок сайтов

## 🎨 Персонализация

### Настройка вкладок и ссылок

Отредактируйте массив `tabs` в файле `userconfig.js`:

```javascript
tabs: [
  {
    name: "Мои сайты",
    links: [
      { name: "GitHub", url: "https://github.com", icon: "ti ti-brand-github" },
      { name: "Gmail", url: "https://gmail.com", icon: "ti ti-mail" }
    ]
  }
]
```

### Настройка поисковых систем

Добавьте или измените поисковые системы в `searchProviders`:

```javascript
searchProviders: [
  { name: "Google", url: "https://www.google.com/search?q=", icon: "ti ti-brand-google", shortcut: "g" },
  { name: "GitHub", url: "https://github.com/search?q=", icon: "ti ti-brand-github", shortcut: "h" }
]
```

### Настройка погоды

```javascript
temperature: {
  location: 'Moscow', // Местоположение по умолчанию
  scale: 'C', // C - Цельсий, F - Фаренгейт
  autoDetect: true // Автоматическое определение местоположения
}
```

## 🌤️ Настройка погоды

### Автоматическое определение местоположения
По умолчанию погода автоматически определяет ваше местоположение. При первом запуске браузер запросит разрешение на доступ к геолокации.

### Ручной ввод города
1. **Кликните на погоду** в нижней панели
2. **Введите название города** (например: "Санкт-Петербург", "London", "Paris")
3. **Нажмите Enter** для сохранения
4. **Нажмите Escape** для отмены

### Настройка в конфигурации
Отредактируйте `userconfig.js`:
```javascript
temperature: {
    location: 'Ваш город', // Укажите ваш город
    scale: 'C', // C - Цельсий, F - Фаренгейт
    autoDetect: true, // Автоматическое определение
    // ...
}
```

### Поддерживаемые города
- Любые города мира
- Названия на русском и английском языках
- Автоматическое определение по GPS

## 🐛 Устранение неполадок

### Проблема: Страница пустая
- Проверьте, что файл `userconfig.js` существует и содержит корректную конфигурацию
- Откройте консоль браузера (F12) и проверьте наличие ошибок

### Проблема: Погода не загружается
- Проверьте подключение к интернету
- Убедитесь, что API ключ корректен (если используется)
- Попробуйте ввести город вручную

### Проблема: Иконки не загружаются
- Проверьте подключение к интернету
- Некоторые сайты могут блокировать загрузку иконок

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE) для подробностей.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка

Если у вас есть вопросы или проблемы:
- Создайте Issue в GitHub
- Проверьте документацию в папке `docs/`
- Обратитесь к разделу "Устранение неполадок"

---

**Наслаждайтесь вашей персонализированной стартовой страницей! 🌅** 